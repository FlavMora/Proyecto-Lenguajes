spring.application.name=Ferreteria

# Puerto del servidor (80 es el puerto estándar HTTP)
server.port=80

# Formato de la fecha en los logs (hora:minuto)
logging.pattern.dateformat=hh:mm

# Eliminar banner de Spring Boot al iniciar
spring.main.banner-mode=off

# Deshabilitar cache de Thymeleaf para desarrollo
spring.thymeleaf.cache=false

# Configuración de conexión a Oracle
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
spring.datasource.username=usuario
spring.datasource.password=clave
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Configuración JPA 
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
